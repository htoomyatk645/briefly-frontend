---
description: Briefly Frontend project conventions
globs:
alwaysApply: true
---

This workspace is **Briefly Frontend** only.

## Structure
- Web prototype: `web/` (React + Vite + Framer Motion)
- Mobile app: `mobile/` (Expo + React Native)
- Shared tokens: `packages/tokens/` and `web/src/tokens.ts`
- Design canvases: `canvases/`

## Design — Warm Editorial (canonical)
- **Light:** warm cream bg `hsl(40 20% 97%)`, primary accent `#C9184A`
- **Dark:** warm near-black bg `hsl(20 6% 7%)`, primary accent `#FF6B8E`
- **Feed zone:** `#18110F` background
- Fonts: Fraunces (display), Geist (body) — never Inter or Roboto as primary
- **No glassmorphism, no gradients, no gradient text**
- No purple gradients, no teal-on-dark, no Spotify-green
- All colors/spacing from `tokens.ts` / `index.css` — no hardcoded hex values

## Motion
- Easing: cubic-bezier(0.22, 1, 0.36, 1)
- Animate transform and opacity only — never width/height/top/left
- Respect prefers-reduced-motion

## Scope
- Default to `web/` unless the user explicitly asks for mobile
- Do not modify agent-tools/, mcps/, or terminals/
